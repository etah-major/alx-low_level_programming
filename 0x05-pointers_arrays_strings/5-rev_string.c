#include "main.h"

/**
 * print_rev - print reverse characters
 * @s: validate the character
 * Return: always 0
 */

void print_rev(char *s)
{
	int = 0;

	while (s[i] != '\0')
	{
		i++;
	}
	i--;
	for (; i >= ; i--)
	{
		_putchar(s[i]);
	}
	_putchar('\n');
}
