#include <stdio.h>
/**
 * main - print if positive, zero, or negative
 *
 * Description: using the maain function
 * this program prints "Programming is positive, zero, or negative
 * Return: 0
 */
int main(void)
{
	char c;
	for (c = 'z' ; c >= 'a' ; c--)
	{
		putchar(c);
	}
	putchar('\n');
	return (0);
}
